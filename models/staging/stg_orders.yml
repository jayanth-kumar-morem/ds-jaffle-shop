models:
  - name: stgOrders
    description: Order data with basic cleaning and transformation applied, one row per order.
    data_tests:
      - dbt_utils.expression_is_true:
          expression: "orderTotal - taxPaid = subtotal"
    columns:
      - name: orderId
        description: The unique key for each order.
        data_tests:
          - not_null
          - unique