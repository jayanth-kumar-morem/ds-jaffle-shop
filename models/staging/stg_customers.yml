models:
  - name: stg_blessed_users
    description: Blessed user data with basic cleaning and transformation applied, one row per blessed user.
    columns:
      - name: blessed_user_id
        description: The unique key for each blessed user.
        data_tests:
          - not_null
          - unique